import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata = {
  title: "Orbit: NASA Space Apps Submission",
  description: "Orbit is an open-source web app built with Next.js, Three.js, and Go. Developed for NASA Space Apps Cairo 2024, it offers user management, visit tracking, and Near-Earth Object (NEO) data handling, with a focus on speed and efficiency. Contributions open after the hackathon.",
};



export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
